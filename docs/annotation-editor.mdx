---
name: 'AnnotationEditor 标注工具'
---

import AnnotationEditor, { AnnotationEditorMode } from '@/components/annotation-editor';
import TestImage from './test.png';
import { useRef, useEffect, useState } from 'react';
import Button from '@/components/button';

export default function  Editor() {
  const ref = useRef();
  let instance = null;
  const tools = [
    { label: '移动画布', handler: () => { instance.setMode(AnnotationEditorMode.DragImage) } },
    { label: 'Alt 缩放画布', handler: () => { instance.setMode(AnnotationEditorMode.Scale) } },
    { label: '矩形', handler: () => { instance.setMode(AnnotationEditorMode.Rect) } },
    { label: '多边形', handler: () => { instance.setMode(AnnotationEditorMode.Polyline) } },
    { label: '移动图形', handler: () => { instance.setMode(AnnotationEditorMode.Edit) } },
    { label: '选中图形', handler: () => { instance.setMode(AnnotationEditorMode.Select) } },
    { label: '多模式', handler: () => { instance.setMode([AnnotationEditorMode.DragImage, AnnotationEditorMode.Scale, AnnotationEditorMode.Edit, AnnotationEditorMode.Select]) } },
    { label: '裁剪选中图形', handler: () => { instance.clipImage() } }
  ];
  useEffect(() => {
    instance = new AnnotationEditor(ref.current);
    instance.init({
      imgUrl: TestImage,
      mode: AnnotationEditorMode.DragImage,
      select: (grp, data) => {
        console.log(data);
      }
    });
  }, []);
  return (
    <div>
      <div>
        {
          tools.map((tool, index) => {
            return (
              <Button key={index} onClick={tool.handler} style={{marginRight: '10px'}}>{tool.label}</Button>
            )
          })
        }
      </div>
      <div style={{width: '100%', height: '800px'}} ref={ref}></div>
    </div>
  )
}