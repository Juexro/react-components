import AnnotationEditor from 'docs/annotation-editor.mdx';
import Button from 'docs/button.mdx';

const components = [
  { component: AnnotationEditor, path: '/components/annotation-editor', exact: true },  { component: Button, path: '/components/button', exact: true },
];

export default components;